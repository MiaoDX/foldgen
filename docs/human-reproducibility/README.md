# Human Reproducibility Records

M4 requires real folding attempts. A simulator-valid FOLD file, deterministic
preview, critic score, or web demo screenshot is not enough to call a case human
reproducible.

Create one JSON record per attempt using `attempt-template.json`. Keep records
out of public launch claims until the attempt status is `pass`.
